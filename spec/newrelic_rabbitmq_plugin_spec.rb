require 'spec_helper'

describe NewrelicRabbitmqPlugin do
  it 'has a version number' do
    expect(NewrelicRabbitmqPlugin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
